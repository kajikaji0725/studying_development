name: Go

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create dot env file
      - shell: bash
        run: |
          touch .env
          echo "name=${{ secrets.name }}" >> .env
